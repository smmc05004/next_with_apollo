type User {
  user_num: Int
  user_id: String
  user_name: String
}

type Query {
  users: [User]
  user(user_id: String!): User
}

type Mutation {
  addUser(user_id: String!, user_name: String!): User
  login(user_id: String!): User
}
