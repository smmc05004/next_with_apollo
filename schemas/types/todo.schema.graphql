# type User {
#   user_num: Int
#   user_id: String
#   user_name: String
# }

type Todo {
  todo_id: Int
  todo: String
  user_num: Int
  user: User
}

type Query {
  todos: [Todo]
  todoById(todo_id: Int!): Todo
}
